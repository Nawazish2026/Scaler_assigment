# Backend Environment Variables

# Database connection string (PostgreSQL)
# For local development:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/calendly_clone?schema=public"
# For Render (use Internal Database URL):
DATABASE_URL="postgresql://user:password@host:5432/database_name"

# Server port (optional, defaults to 3000)
PORT=3000

# Frontend URL for CORS (set this to your Vercel frontend URL in production)
# Leave empty or set to * for development
FRONTEND_URL=https://your-frontend.vercel.app
